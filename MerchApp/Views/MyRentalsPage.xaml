<Page
    x:Class="MerchApp.Views.MyRentalsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:models="using:MerchApp.Models"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid RowSpacing="16" Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackPanel Grid.Row="0" Spacing="4">
            <TextBlock
                Text="My Rentals"
                Style="{StaticResource TitleLargeTextBlockStyle}"/>
            <TextBlock
                Text="Track your rental requests and returns"
                Style="{StaticResource CaptionTextBlockStyle}"
                Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
        </StackPanel>

        <!-- Filter tabs -->
        <StackPanel
            Grid.Row="1"
            Orientation="Horizontal"
            Spacing="8">
            <RadioButton
                Content="All"
                IsChecked="True"
                Command="{x:Bind ViewModel.FilterByCommand}"
                CommandParameter="All"/>
            <RadioButton
                Content="Pending"
                Command="{x:Bind ViewModel.FilterByCommand}"
                CommandParameter="Pending"/>
            <RadioButton
                Content="Active"
                Command="{x:Bind ViewModel.FilterByCommand}"
                CommandParameter="Active"/>
            <RadioButton
                Content="Returned"
                Command="{x:Bind ViewModel.FilterByCommand}"
                CommandParameter="Returned"/>
        </StackPanel>

        <!-- Loading -->
        <ProgressRing
            Grid.Row="2"
            IsActive="{x:Bind ViewModel.IsBusy, Mode=OneWay}"
            HorizontalAlignment="Center"
            VerticalAlignment="Center"/>

        <!-- Empty state -->
        <StackPanel
            Grid.Row="2"
            Spacing="12"
            HorizontalAlignment="Center"
            VerticalAlignment="Center"
            Visibility="{x:Bind ViewModel.IsEmpty, Mode=OneWay, Converter={StaticResource BoolToVisibilityConverter}}">
            <FontIcon
                Glyph="&#xE7BC;"
                FontSize="48"
                Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
            <TextBlock
                Text="No rentals yet"
                Style="{StaticResource SubtitleTextBlockStyle}"
                HorizontalAlignment="Center"/>
            <TextBlock
                Text="Browse the catalogue and submit your first request"
                Style="{StaticResource CaptionTextBlockStyle}"
                Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                HorizontalAlignment="Center"/>
        </StackPanel>

        <!-- Requests list -->
        <ListView
            Grid.Row="2"
            ItemsSource="{x:Bind ViewModel.FilteredRequests, Mode=OneWay}"
            SelectionMode="None"
            Visibility="{x:Bind ViewModel.IsEmpty, Mode=OneWay, Converter={StaticResource BoolNegationToVisibilityConverter}}">

            <ListView.ItemTemplate>
                <DataTemplate x:DataType="models:RentalRequest">
                    <Border
                        Padding="16"
                        Margin="0,4"
                        CornerRadius="8"
                        Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                        BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                        BorderThickness="1">

                        <Grid RowSpacing="8">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>

                            <!-- Status + dates -->
                            <Grid Grid.Row="0">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>

                                <TextBlock
                                    Grid.Column="0"
                                    Style="{StaticResource CaptionTextBlockStyle}"
                                    Foreground="{ThemeResource TextFillColorSecondaryBrush}">
                                    <Run Text="{x:Bind RentalFrom}"/>
                                    <Run Text="→"/>
                                    <Run Text="{x:Bind RentalTo}"/>
                                </TextBlock>

                                <TextBlock
                                    Grid.Column="1"
                                    Text="{x:Bind StatusDisplayName}"
                                    Style="{StaticResource CaptionTextBlockStyle}"
                                    Foreground="{x:Bind Status, Converter={StaticResource StatusToColorConverter}}"/>
                            </Grid>

                            <!-- Items summary -->
                            <TextBlock
                                Grid.Row="1"
                                Style="{StaticResource BodyStrongTextBlockStyle}"
                                TextTrimming="CharacterEllipsis"/>

                            <!-- Manager note if rejected -->
                            <TextBlock
                                Grid.Row="2"
                                Style="{StaticResource CaptionTextBlockStyle}"
                                Foreground="{ThemeResource SystemFillColorCriticalBrush}"
                                TextWrapping="Wrap"/>

                        </Grid>
                    </Border>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>

    </Grid>
</Page>